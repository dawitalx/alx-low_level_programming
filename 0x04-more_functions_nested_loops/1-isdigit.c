#include "main.h"

/**
 *_isdigit - check for digit
 *@n: check intiger n
 *return: 1 if digit otherwise 0
 */

int _isdigit(int n)
{
	if ( n >= 0 && n <=9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
