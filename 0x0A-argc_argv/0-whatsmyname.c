#include "main.h"
#include <stdio.h>

/**
 * main - prints programs name
 * @argc: number of commands
 * @argv: array of command
 */

int main(int argc, char *argv[])
{
	if (args > 0)
		printf("%d", argv[0]);

	return (0);
}
