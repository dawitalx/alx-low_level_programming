#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of string.
 * @s: characted to be checkd.
 * Return: the number of string.
 */

int _strlen(char *s)
{
	return (strlen(*s));
}
