#include "main.h"

/**
 * _isdigit - cheks for digits.
 * @n: intiger 
 * return: 1 if it is adigit other wise 0
 */
int _isdigit(int n)
{
	if ( n >= 0 && n <=9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
